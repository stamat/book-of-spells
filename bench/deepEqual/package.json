{
  "name": "bench-deepequal",
  "description": "Benchmarks for deepEqual(). Not published, not part of the library — this manifest exists so the rivals land here and nowhere near the root.",
  "private": true,
  "scripts": {
    "setup": "npm install --no-audit --no-fund",
    "bench": "sh -c 'for f in *.bench.mjs; do node \"$f\" \"$@\"; done' --",
    "teardown": "rm -rf node_modules package-lock.json"
  },
  "devDependencies": {
    "deep-eql": "5.0.2",
    "es-toolkit": "1.50.0",
    "fast-deep-equal": "3.1.3",
    "lodash": "4.18.1",
    "ramda": "0.32.0"
  }
}
